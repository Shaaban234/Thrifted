# Copy to .env and fill in.
# The app runs on mock data without Supabase — those are only needed for a real backend.
# WARNING: EXPO_PUBLIC_* vars are bundled into the app and readable by users.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Demo admin account — logging in with these credentials grants admin access.
EXPO_PUBLIC_ADMIN_EMAIL=shabanabdullah0@gmail.com       
EXPO_PUBLIC_ADMIN_PASSWORD=123456789012
